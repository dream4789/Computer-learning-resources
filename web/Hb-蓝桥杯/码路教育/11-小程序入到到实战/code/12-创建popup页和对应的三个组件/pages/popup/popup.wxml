<van-search 
  value="{{ searchVal }}"
  placeholder="请输入搜索关键词"
  show-action
  bind:search="onSearch"  
  bind:cancel="onCancel"
/>

<HistoryHot wx:if="{{blockShow == 1}}"></HistoryHot>
<TipsList  wx:elif="{{blockShow == 2}}"></TipsList>
<ProductList wx:else=""></ProductList>