<view class="tab-control">
  <block wx:for="{{tabbarData}}" wx:key="*this">
    <view 
      bindtap="itemTap" 
      data-index="{{index}}" 
      class="item {{index === currentIndex ? 'active' : ''}}">
      <text class="title">{{item}}</text>
    </view>
  </block>
</view>